anthropic>=0.40.0
requests>=2.31.0
openpyxl>=3.1.0
xlrd>=2.0.1          # reads NYU Stern's legacy .xls benchmark files
pytest>=7.4.0

# Optional — only needed to run the web UI (webapp/). The CLI and tests don't use these.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0
